# No dependencies. Both transports are standard library only: json, subprocess,
# threading, socket, http.server, http.client, sys. This file exists so the run
# instructions are uniform across chapters; there is nothing to install.

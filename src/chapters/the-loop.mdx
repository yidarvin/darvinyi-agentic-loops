import { TheLoopFigure } from "./_figures/TheLoopFigure";
import { TheLoopWidget } from "./_widgets/TheLoopWidget";

An agent is a loop. Not a model, not a prompt, not a framework: a loop that calls a
model, runs whatever the model asked for, feeds the result back, and calls the model
again until there is nothing left to do. The word "agent" names that cycle, not the
network inside it. This is the whole book compressed into one sentence, and every
later chapter is an argument about how to build the loop well: what to put in the
context, when to stop, how to remember, when to split the work. Start here, because if
the loop is clear, the rest is engineering.

## The loop is the primitive

The cycle has four phases. **Perceive**: the harness assembles a prompt from the
system instructions, the tool definitions, and the running message list. **Decide**:
the model runs one forward pass and emits text, tool-use requests, or both. **Act**:
the harness parses the requests, checks permissions, and runs the real tools.
**Observe**: the tool results are appended to the message list, and the cycle repeats.

This is an old shape wearing new clothes. It descends directly from sense-plan-act,
the deliberative architecture behind Shakey the robot at Stanford Research Institute
in the late 1960s: perceive the world, update a model of it, plan, act, repeat. The
difference is what fills the boxes. The "world model" is now the accumulated text in
the context window, and the "planner" is a frozen neural policy that you cannot
retrain in the loop. Anthropic's definition is the smallest correct one the field has
converged on: agents "are typically just LLMs using tools based on environmental
feedback in a loop." The decide phase is where reasoning and action interleave, the
pattern Yao and colleagues named ReAct in 2022.

<Figure id="1.1" caption="One turn of the loop. The model is called exactly once, in decide; every other arrow is the harness. The message list at the center is the only place state accumulates.">
  <TheLoopFigure />
</Figure>

## The model is a stateless function

The model remembers nothing between calls. It is a pure function from a prompt to the
next block of output, and the prompt is whatever the harness chose to assemble this
turn. Everything that feels like memory, continuity, or progress lives in one place:
the running message list. That list is the agent's working memory. The system prompt,
the tool schemas, the conversation, and every tool result share a single context
window, and the list grows in exactly two places. It grows on **decide**, when the
assistant's turn is appended, and on **observe**, when the tool result is appended.
Perceive only reads the list; act only touches the outside world.

Because the policy is frozen, the only lever you actually control is the context you
construct. Barry Zhang of Anthropic reduces an agent to three design decisions, the
environment, the tools, and the system prompt, and then "the model gets called in a
loop, and that's agents." Here is that loop with nothing removed:

```python
from anthropic import Anthropic

client = Anthropic()
messages = [{"role": "user", "content": "What files are here?"}]

while True:
    reply = client.messages.create(
        model="claude-sonnet-5", max_tokens=1024,
        tools=TOOLS, messages=messages,
    )
    messages.append({"role": "assistant", "content": reply.content})  # verbatim
    if reply.stop_reason != "tool_use":
        break
    results = [
        {"type": "tool_result", "tool_use_id": b.id, "content": run_tool(b.name, b.input)}
        for b in reply.content if b.type == "tool_use"
    ]
    messages.append({"role": "user", "content": results})
```

Two API invariants hold this together. Append the assistant turn verbatim, so the
`tool_use` IDs the model generated stay aligned with the results you send back. And
put the `tool_result` blocks in a user message that immediately follows their
`tool_use` blocks. Break either invariant and the next call rejects the history. Note
what is absent: there is no planner, no memory store, no orchestration graph. The
`while` is the agent.

## Termination belongs to the harness

Look again at the one branch in that loop: it stops when `stop_reason` is not
`tool_use`. Deciding when to stop is a harness responsibility, and it has to be,
because models are unreliable at knowing when they are done. Left alone, an agent will
declare victory early or grind in circles late. So the harness owns the stop
conditions, and there are five worth naming: the model requests no tool, an explicit
completion signal fires, a hard iteration limit is reached, a cost or token or
wall-time budget is spent, or an error state is hit. The Anthropic Messages API
surfaces the first of these as `stop_reason`: `tool_use` means the model wants a tool,
`end_turn` means it is finished, `max_tokens` means the response was truncated and may
have left an incomplete tool call to retry. A production loop wires the other four
around that signal as ordinary control flow.

## Watch the loop turn

Stepping through one run makes the shape concrete. The trajectory below asks a tiny
agent how many lines are in a file. Advance it one phase at a time and watch two
things: the phase ring cycling perceive to decide to act to observe, and the message
list growing only on decide and observe. The run halts when the model returns
`end_turn` instead of a tool request, and that halt is the harness's call, not the
model's.

<Widget name="loop_stepper" hint="Step through one run. The ring shows the active phase; the message list grows on decide and observe; halt is the harness exiting.">
  <TheLoopWidget />
</Widget>

## Minimal is startlingly strong

The unsettling part is how little you need. mini-swe-agent, from the SWE-bench team at
Princeton and Stanford, is roughly a hundred lines of Python. It uses only bash, no
tool-calling API at all, so it runs with any model, and it scores above 74 percent on
SWE-bench Verified (74.2 percent with Gemini 3 Pro in the Live-SWE-agent report,
arXiv:2511.13646). A hundred lines of loop, competitive on a hard real-world coding
benchmark. The team's own conclusion is blunt: "the interface is the model." When the
harness is thin, the score is a measurement of the model, which is exactly why
SWE-bench fixes a minimal harness to compare models fairly.

The same lesson shows up in the reference implementation. Community reverse-engineering
of Claude Code describes a single while-loop at the center, with rough estimates of
around 1.6 percent of the codebase doing AI decision logic and around 98.4 percent
doing operational harness work: permissions, compaction, steering, persistence. Treat
those exact numbers as indicative rather than official, but the ratio is the point.
The intelligence is bought; the reliability is built.

This does not mean scaffolding is worthless. The debate is live: a well-designed
agent-computer interface, a windowed file viewer plus a targeted edit tool, has been
shown to roughly double a raw-bash score on SWE-bench. As base models strengthen, the
marginal value of clever scaffolding shrinks, while harness engineering still decides
reliability, cost, and safety. Build the loop first. Add harness, not decoration.

That also tells you when to reach for an agent at all. A **workflow** runs LLMs
through control flow you wrote in advance; you own the path. An **agent** lets the
model direct its own tool use; the model owns the path. If you can draw the decision
tree, write the workflow. Reserve the agent for tasks that are ambiguous enough that
you cannot pre-map the path, valuable enough to justify the token spend, and forgiving
enough that a wrong step is detectable or cheap. Coding fits all three, which is why it
became the exemplary agent use case.

## Run the loop yourself

The artifact for this chapter is the four-phase loop as real, runnable code. It prints
a banner for every phase on every turn, so you can watch the message list grow and see
the harness decide to halt. It runs with no setup: with no API key it falls back to a
deterministic offline model that replays a scripted trajectory, so the loop still
turns. Set a key and the identical loop runs against a live model. Only the model
swaps; the loop is the same, which is the chapter's thesis in executable form.

<RunnableArtifact
  name="loop_runner"
  path="artifacts/ch01-the-loop/"
  runtime="python 3.9+"
  run={["cd artifacts/ch01-the-loop", "python3 loop.py"]}
  requires="ANTHROPIC_API_KEY (optional; runs offline without it)">
A minimal agent loop over the Messages API that logs perceive, decide, act, and
observe as it iterates, then halts when the model stops requesting tools.
</RunnableArtifact>

## Where loops break

If the loop is this simple and this strong, the rest of the book is about the ways it
fails at scale. Three failures are structural and set up everything that follows.
Context accumulates: every turn adds tokens, and tool outputs balloon fastest, so an
agent can run out of room after only a handful of calls. Errors cascade: one
wrong step feeds the next, and per-step error rates multiply across a long chain.
Coherence decays: attention degrades over long contexts, the lost-in-the-middle
effect, so a model can forget an instruction it was given while the window is still
half empty.

Each failure has a chapter aimed at it. Context economics is the master constraint of
Part I. Memory, in Part IV, is the set of answers to accumulation and decay. Splitting
work across isolated agents, in Part III, is one answer to cascades, and its own
source of new failures. All of it is engineering on top of the loop you just built.

## Exercises

<ExerciseCard n="01" title="Add a third tool">
In `loop.py`, add a `search(pattern)` tool that greps the files in the directory, and
change the task to something that needs it (for example, "which file defines
run_tool?"). Run it and confirm the loop takes an extra turn. You changed the agent's
behavior without touching the loop.
</ExerciseCard>

<ExerciseCard n="02" title="Give the harness a budget">
Add a hard cap on total tool calls, separate from `MAX_TURNS`. When the cap is hit,
append a `tool_result` that says so and let the model wrap up. Confirm the model cannot
spend past the budget. Termination is yours, not the model's.
</ExerciseCard>

<ExerciseCard n="03" title="Break termination on purpose">
Comment out the `stop_reason != "tool_use"` check and give the agent a task that needs
no tool. Watch it run to `MAX_TURNS` instead of halting. This is why the stop condition
lives in the harness: without it, the loop never ends on its own.
</ExerciseCard>

<ExerciseCard n="04" title="Watch context grow">
Print an estimated token count on every perceive phase (a rough proxy is
`len(json.dumps(messages)) // 4`). Run a few turns and watch it climb monotonically.
That curve is the subject of the next chapter on context-window economics.
</ExerciseCard>

## Sources

- Anthropic, "Building Effective Agents" (December 2024). The loop definition and the agent-versus-workflow distinction.
- Yao et al., "ReAct: Synergizing Reasoning and Acting in Language Models" (2022), arXiv:2210.03629.
- mini-swe-agent, the SWE-bench / SWE-agent team (Princeton and Stanford). Roughly 100 lines, bash-only.
- "Live-SWE-agent" (2025), arXiv:2511.13646, for the SWE-bench Verified figures cited here.
- Jimenez et al., "SWE-bench: Can Language Models Resolve Real-World GitHub Issues?" (2024), arXiv:2310.06770.
- Barry Zhang (Anthropic), public talk on building agents: environment, tools, system prompt, then a loop.
- Anthropic Messages API documentation, for `stop_reason` semantics (`tool_use`, `end_turn`, `max_tokens`).
- Community reverse-engineering analyses of Claude Code (the single-loop design and the 1.6 / 98.4 percent estimates); indicative, not official architecture documentation.

import { MemoryTaxonomyFigure } from "./_figures/MemoryTaxonomyFigure";
import { MemoryTaxonomyWidget } from "./_widgets/MemoryTaxonomyWidget";

An agent has no memory in the way a process has memory. Each model call sees only the
context its harness supplies. Durable behavior begins when the harness persists useful
state outside that window, retrieves a small relevant slice, and puts that slice back into
the next call. The taxonomy matters because working, episodic, semantic, and procedural
memory solve different failures. Treating them as one generic vector store produces stale
context, unsafe writes, and agents that remember the wrong thing.

## Working memory is the live context

Working memory is the current context window: the active conversation, recent tool
results, task state, and retrieved material. It is immediately available because it is
already in the request. It is also ephemeral, token-expensive, and bounded. When the
session ends or the harness compacts history, that working set is gone unless something
was written elsewhere.

This continues the constraint from [Context-Window Economics](/context-window-economics).
The window is not a durable database with a generous cache policy. Every extra memory
record competes with instructions, evidence, tools, and output space. A long context full
of weakly relevant recollections can perform worse than a short one with the right records.

Do not confuse parametric knowledge with agent memory. Model weights may contain broad,
static knowledge learned during training. Agent memory is dynamic external state for this
user, task, or organization. It can be scoped, inspected, updated, and deleted. That
control plane is the reason production agents usually place mutable information outside
the weights.

## Four stores answer four questions

CoALA gives the useful map: one short-term working store and three long-term stores. The
boundaries are engineering boundaries, not a claim that cognition has four sealed boxes.
An episode can become a fact through consolidation, and a fact can later shape a rule. The
questions below expose the distinction that matters when designing reads and writes.
Here, short-term and working memory are two names for the same context-window layer.
Long-term means the persistent stores outside it.

- **Working memory** answers: what is active in this call? It is the context window and
  its scratch state.
- **Episodic memory** answers: what happened? It keeps dated interactions, trajectories,
  tool outcomes, and successful examples with their original circumstances.
- **Semantic memory** answers: what is true? It holds decontextualized facts, profiles,
  preferences, and durable knowledge.
- **Procedural memory** answers: how do we act? It lives primarily in system prompts,
  skills, code, tests, and model weights. A portable skill is explicit procedural memory,
  not a record waiting in a similarity index.

<Figure id="15.1" caption="Working memory is the context-window hub. Episodic, semantic, and procedural stores persist outside it, but only a selected retrieval changes the next generation. Consolidation turns a dated episode into a compact fact; procedural rules enter the window as instructions, skills, or code.">
  <MemoryTaxonomyFigure />
</Figure>

Consider three records from a release assistant. "Mira said she eats vegetarian food" is
an episode. "Mira prefers vegetarian food" is a semantic fact distilled from that episode.
"Run migration verification before production deployment" is a procedure when it is
enforced by a release skill, a CI check, or an instruction. The same sentence may pass
through several forms, but each form has a different retrieval path and a different
write-risk profile. Editing a user profile is reversible. Editing a deployment procedure
can change future behavior for everyone.

## Memory is a lifecycle, not a database choice

Every design has the same movement: encode a candidate, store it with metadata, retrieve
it into working memory, then update, forget, or consolidate it. The hard decision is not
where to put text. It is which candidate deserves a future context slot.

Write records with a type, scope, provenance, timestamp, and retention rule. Scope is
usually per user or per session. Shared multi-agent memory needs an explicit owner and
writer policy, because one polluted record can contaminate every downstream agent.
Provenance lets an operator trace and remove a bad record. A timestamp lets a system tell
"Mira preferred vegetarian food last year" from a current preference.

Retrieval should follow the question. Vector search works well for fuzzy recall over
episodes or facts. A key-value or document store suits an exact profile. A temporal graph
earns its cost when facts change over time and point-in-time answers matter. Files are
often the simplest durable scratchpad because an agent and an operator can inspect them
directly. None of these substrates make memory useful by themselves. A record still has to
win a retrieval decision and fit the next context window.

Forgetting is equally architectural. A profile may overwrite a superseded address. An
audited system may append a new value and close the old value's validity range. A history
may compress many episodes into one semantic note. The right policy depends on whether the
next question is "what is current?" or "what did we know then?". Start with context
management or direct retrieval from an existing source of truth. Add long-term memory only
when cross-session continuity, personalization, or long-horizon learning needs it.

<Callout>
Persistent memory is a security boundary. Namespace it per user, restrict who can write,
keep provenance, and filter sensitive fields before storage. Memory poisoning survives the
turn that introduced it, so a permissive write path can turn an untrusted sentence into a
future instruction.
</Callout>

## Product examples are implementations, not the taxonomy

**Product examples.** MemGPT and Letta frame the context window and external stores as an
OS-like hierarchy. LangMem exposes episodic, semantic, and procedural memory as first-class
concepts. Mem0 emphasizes extracted user facts, while Zep and Graphiti emphasize temporal
facts and provenance. These are useful designs, but none changes the underlying test:
identify the question, choose the store and lifecycle that answer it, then measure whether
the retrieval earns its context cost.

The evidence should include accuracy, token cost, and latency together. LongMemEval and
LoCoMo exist because retaining information across many sessions remains difficult. Vendor
benchmarks can show a direction, but headline comparisons often include different retrieval
pipelines, embeddings, and judge models. Use them to generate a hypothesis, then test the
memory policy against the agent's own tasks.

## Switch the regime

The same conversation can produce different future behavior depending on what survives the
session boundary. Select a regime below. The readout shows the retained representation, the
question it can answer, and the information it deliberately leaves behind.

<Widget name="memory_taxonomy" hint="Switch among working, episodic, semantic, procedural, and combined regimes. Watch the same prior interaction become unavailable, a dated trace, a compact fact, a release rule, or a coordinated set of all four.">
  <MemoryTaxonomyWidget />
</Widget>

## Run the same session five ways

The runnable harness makes the distinction executable without an API key. It replays one
short interaction under five regimes and asks three follow-up questions. The offline model
is intentionally deterministic: it isolates the memory policy instead of mixing it with
model variation. Use it as a design probe, then replace its records with a trace from an
agent you operate.

<RunnableArtifact
  name="memory_regime_harness"
  path="artifacts/ch15-memory-taxonomy/"
  runtime="node 18+"
  run={["cd artifacts/ch15-memory-taxonomy", "node memory_harness.mjs --compare", "bash check.sh"]}>
A self-contained session harness that holds one interaction fixed, switches which stores
survive a session boundary, and prints the resulting answers with the consulted memory
type. It uses no network or API key.
</RunnableArtifact>

## Exercises

<ExerciseCard n="01" title="Classify one agent's state">
Choose an agent you run. List six pieces of state it carries, then label each working,
episodic, semantic, or procedural. For each ambiguous item, state the question it must
answer and the next action that would write it. Do not pick a substrate until the type is
clear.
</ExerciseCard>

<ExerciseCard n="02" title="Make a changing preference auditable">
Design the record for a user preference that can change, such as dietary restrictions or a
deployment window. Specify its namespace, source event, timestamp, current-value rule,
retention period, and the operator action that can remove it. Decide whether update means
overwrite, append, or invalidate.
</ExerciseCard>

<ExerciseCard n="03" title="Run a failed trace through the taxonomy">
Run the harness, replace its deployment incident with a real failed task trace, and rerun
the five regimes. Decide which parts should remain as episodes, consolidate into facts,
become a tested procedure, or be discarded. Defend the procedural write especially.
</ExerciseCard>

<ExerciseCard n="04" title="Put a fence around shared memory">
Sketch a shared store for three cooperating agents. Name the namespace, the single writer
or merge rule, the provenance fields, the review path for a new record, and the retrieval
budget per turn. Then name one poison record your design rejects before it reaches another
agent.
</ExerciseCard>

## Sources

- [Sumers, Yao, Narasimhan, and Griffiths, "Cognitive Architectures for Language Agents" (CoALA)](https://arxiv.org/abs/2309.02427), for the working, episodic, semantic, and procedural taxonomy and the differing risks of procedural writes.
- [Park et al., "Generative Agents: Interactive Simulacra of Human Behavior"](https://arxiv.org/abs/2304.03442), for the memory-stream pattern of timestamped episodes, scored retrieval, and reflection.
- [Packer et al., "MemGPT: Towards LLMs as Operating Systems"](https://arxiv.org/abs/2310.08560), for the context-window and external-store hierarchy.
- [Wu et al., "LongMemEval: Benchmarking Chat Assistants on Long-Term Interactive Memory"](https://arxiv.org/abs/2410.10813), for the evaluation dimensions and sustained-interaction challenge.
- [Chhikara et al., "Mem0: Building Production-Ready AI Agents with Scalable Long-Term Memory"](https://arxiv.org/abs/2504.19413) and [Rasmussen et al., "Zep: A Temporal Knowledge Graph Architecture for Agent Memory"](https://arxiv.org/abs/2501.13956), for representative product-oriented memory designs and their vendor-reported evaluation results.

# ch{{NUM}} - {{TITLE}}

This directory contains the runnable companion to **{{TITLE}}**.

## Run

Document the exact local command, runtime, configuration requirements, and expected
output here. Keep the example self-contained where possible and fail clearly when an
optional external dependency is unavailable.

## Deterministic check

`check.sh` is executed by `npm run check`. Replace its scaffold with a deterministic
command that proves the artifact's central behavior without needing network access or
credentials.

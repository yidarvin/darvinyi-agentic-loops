# Compatibility pointer

This repository's canonical project instructions are in [AGENTS.md](AGENTS.md).
Use the `refsite-runner` workflow and `./run.sh`; do not revive the legacy Claude
queue loop or its settings-based invocation.

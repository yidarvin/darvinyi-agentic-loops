verdict: resolved

## Round 1 review (2026-07-16)
Fresh-eyes review: read the complete current chapter, its figure and widget, the full runnable artifact and its bundled skill, the build notes, research reference, registry state, and the empty prior critique history. Ran npm run check successfully, ran the artifact's check.sh successfully, exercised its documented modes plus unsupported tags and the CLI plus live-data decision path, and validated the bundled skill with the chapter 10 portable validator. Checked the linked primary specifications and product sources, including Agent Skills, MCP architecture, transport and authorization specifications, Anthropic Tool Search material, Claude Code MCP documentation and changelog, Simon Willison, Armin Ronacher, Sentry, and JFrog.

## Required fixes
1. **src/chapters/_widgets/SkillOrServerWidget.tsx, artifacts/ch11-skill-or-server/hybrid_lab.py, src/chapters/skill-or-server.mdx, and artifacts/ch11-skill-or-server/README.md: live data incorrectly overrides already available CLI or server access.** Running python3 hybrid_lab.py --decide --access --live --cli-exists reports SERVER and says no existing tool covers the access, despite printing cli_or_existing yes. Exercise 03 teaches the same rule. Fresh data requires a fresh fetch, but a CLI or existing server can supply that fetch, so this contradicts the chapter's own CLI correction and makes the signature widget and artifact route a core case falsely. Make the classifier distinguish missing live access from live access already supplied by a CLI or existing server, mirror the corrected rule in the widget and prose, and add regression coverage for both access plus live plus cli-exists cases. Ensure the mini-plane represents the effective access and judgment state after this correction rather than contradicting the selected inputs.
2. **artifacts/ch11-skill-or-server/commit_server.py and hybrid_lab.py: list_commits_since does not honor its advertised tag argument.** The tool, CLI help, README, and chapter promise commits since the supplied tag, but python3 hybrid_lab.py --server-only --tag does-not-exist returns the fixture's v0.3.0 to v0.4.0 range and merely echoes does-not-exist. The source comment admits this. Either implement truthful fixture ranges or reject unsupported tags as a visible tool error, then test that behavior. A runnable access-layer example cannot silently fabricate a range.
3. **artifacts/ch11-skill-or-server/release_notes_skill/SKILL.md and its containing directory: the claimed real skill is invalid at the artifact path.** The bundled folder is named release_notes_skill while its frontmatter declares name: release-notes. The Agent Skills specification requires the name to match the parent directory. Running python3 artifacts/ch10-skills/skills_lab.py --validate artifacts/ch11-skill-or-server/release_notes_skill produces ERROR P5 for that mismatch, while the README and chapter call it a valid or real skill. Rename the bundled directory to release-notes and update all artifact paths, or otherwise make the checked-in skill valid and add it to the deterministic check.
4. **src/chapters/skill-or-server.mdx Sources and its Tool Search discussion: consequential product claims are conflated or lack their primary source.** The January 2026 10-percent automatic deferral behavior was Claude Code's MCP Tool Search launch, not a generic MCP capability. The cited Anthropic advanced-tool-use page instead documents API Tool Search, where callers opt in with defer_loading, while its 77K to 8.7K and 85-percent figures come from that API example. Label and source the Claude Code behavior precisely, including its historical versus current configuration semantics, instead of presenting it as a vendor-neutral MCP retrofit. Also add the omitted primary sources for the direct Ronacher and Galarza attributions, especially the Ronacher quotation and Sentry 8K/interface-stability account that the chapter relies on. The current Sources list has no Ronacher or Galarza link, violating the chapter's source-link requirement.
5. **src/chapters/skill-or-server.mdx: the Sentry example names stale skills as current.** The linked current Sentry for AI source lists sentry-get-started, sentry-instrument, and sentry-debug-issue as its core skills, not sentry-fix-issues or sentry-code-review. The chapter's present-tense claim therefore sends readers to nonexistent or legacy artifacts and weakens its central layered example. Update the example to the current Sentry skill names and behavior, or date and source a historical version explicitly.

## Advisories
- None.

## Builder resolution (2026-07-16)
Regression gate: read the full critique history with `git log -p -- content/critiques/skill-or-server.md`. Round 1 is the only review, so its five REQUIRED fixes were re-verified against the current chapter, widget, figure, artifact, and research backbone. The figure's shared-governance rule remains intact; no prior-round fix exists outside Round 1.

1. **Live access and the classifier.** Updated `hybrid_lab.py`, `SkillOrServerWidget.tsx`, the chapter, and the artifact README so an existing CLI or server can make a fresh fetch. Both live-plus-CLI cases now route to SKILL and have regression coverage. The widget's mini-plane now draws effective access and procedure needs. Shared governance remains on the server side, with a dedicated regression case, because a local CLI is not the central boundary.
2. **Truthful tag behavior.** Updated `commit_server.py` to reject any fixture tag other than `v0.3.0` with a visible JSON-RPC invalid-parameter error. Added server and CLI regression checks, and documented the truthful fixture range in the chapter and README.
3. **Valid bundled skill.** Renamed `release_notes_skill/` to `release-notes/` to match `name: release-notes`, updated all artifact paths, and added the Chapter 10 portable skill validator to `artifacts/ch11-skill-or-server/check.sh`.
4. **Product claims and primary sources.** Separated historical Claude Code automatic deferral, current Claude Code configuration, and caller-selected API `defer_loading` behavior in the chapter and matching research file. Added direct Ronacher and Galarza sources, and separated Ronacher's Sentry query-syntax experience from his general tool-description warning.
5. **Current Sentry example.** Replaced stale skills with the current Sentry for AI core skills: `sentry-get-started`, `sentry-instrument`, and `sentry-debug-issue`, with their current behavior and source.

No advisories were taken. `artifacts/ch11-skill-or-server/check.sh` passes 22 assertions and validates the bundled skill. `npm run check` passes all seven stages.

## Round 2 review (2026-07-16)
Fresh-eyes re-review: read the complete current chapter, figure, widget, runnable artifact, research record, build notes, registry entry, and the complete critique history with `git log -p -- content/critiques/skill-or-server.md`. Ran `npm run check` successfully, including the artifact's 22 assertions and portable bundled-skill validation. Exercised the documented classifier cases and the unsupported-tag path. Checked the Agent Skills specification, MCP architecture and authorization specifications, current Claude Code Tool Search documentation and changelog, Anthropic's Tool Search guide, Ronacher, Galarza, Sentry for AI, JFrog, and the chapter's other listed sources. Re-verified every Round 1 requirement: live data plus existing access no longer creates a duplicate server, unsupported tags reject visibly, the bundled skill's directory matches its name and validates, the Tool Search distinctions and direct sources remain present, and the Sentry example uses the current core skill names.

## Required fixes
1. **`artifacts/ch11-skill-or-server/hybrid_lab.py:152-225`, `src/chapters/_widgets/SkillOrServerWidget.tsx:52-123`, `artifacts/ch11-skill-or-server/README.md:56-62`, and `src/chapters/skill-or-server.mdx:214-219` --- existing access is incorrectly converted into a missing procedure.** `python3 hybrid_lab.py --decide --access --cli-exists` prints `judgment no`, `cli_or_existing yes`, then returns `SKILL`; the README and Exercise 03 explicitly teach that result. But the artifact defines `--judgment` as the workflow or procedure the agent lacks, and defines `--cli-exists` as access the agent can already use. This case has neither an access gap nor a judgment gap, so the chapter's own four-way model must return `NEITHER` and “build nothing,” or expose a distinct adopt-existing outcome. Do not manufacture a Skill solely because a CLI exists. Correct both mirrored classifiers, the effective mini-plane, documentation, and exercise, and add regression coverage. This is the distinct non-live case, not a reopening of Round 1's settled live-access correction.
2. **`src/chapters/skill-or-server.mdx:48-51` and `docs/research/ch11-skill-or-server.md:7,20` --- the core Skill token model both miscomputes startup cost and turns a recommendation into a runtime invariant.** The chapter says each Skill contributes about 100 metadata tokens, then says 100 installed Skills cost only “a few kilotokens.” The [Agent Skills specification](https://agentskills.io/specification) describes startup metadata as approximately 100 tokens per Skill, which makes that example roughly 10,000 tokens, not a few thousand. The same source calls a body below 5,000 tokens a recommendation, not a maximum a host enforces. Correct the arithmetic and qualify the body size as a design target or use measured listing data; align the research backbone so the central context-economics comparison stays truthful.
3. **`src/chapters/skill-or-server.mdx:141-145`, the decision model, and its artifact documentation --- fresh or authenticated access is falsely presented as requiring an existing CLI/server or a new server.** A bare `SKILL.md` has no built-in connection or credential model, but a Skill can direct the agent to execute a bundled script that calls an API when its runtime supplies network access and credentials. The chapter's own research record says exactly that at `docs/research/ch11-skill-or-server.md:24-30`, and [Galarza's primary account](https://www.damiangalarza.com/posts/2026-02-05-mcps-vs-agent-skills/) explicitly gives workflow-local scripts that hit APIs or run `curl` as a Skill use case. A server is the stronger choice for reusable, shared, stateful, or centrally governed access. It is not the sole valid fresh-access mechanism. State that boundary accurately and update the classifier, figure/widget, artifact, and research record so they either represent script-backed workflow-local access or explicitly scope the model to shared access adapters.

## Advisories
- `artifacts/ch11-skill-or-server/release-notes/references/CONVENTIONS.md:25-30` says a breaking change routes to `Changed`, while `scripts/format_notes.py:43-48` gives `Security` precedence for the exercised `feat(security)!` fixture. State the precedence in the reference document. This does not affect the chapter's Skill-versus-server lesson.
- The exact GitHub MCP token figure in the chapter would benefit from its direct measurement source. The cited Anthropic guide supports the 55,000 and 134,000 examples, but not the 17,600 figure. Non-blocking because the chapter labels these as order-of-magnitude.

## Builder resolution (2026-07-16)
Regression gate: read the complete append-only history with `git log -p -- content/critiques/skill-or-server.md`, then re-verified every REQUIRED finding from Rounds 1 and 2 against the current chapter, figure, widget, artifact, and matching research backbone. `npm run check` passes all seven stages; the Chapter 11 artifact check passes 24 assertions and validates the bundled `release-notes` Skill.

1. **Round 2, existing access is not a missing procedure.** Updated `artifacts/ch11-skill-or-server/hybrid_lab.py` and `src/chapters/_widgets/SkillOrServerWidget.tsx` so existing CLI or server access with no judgment gap returns `NEITHER`, including the live-data case. A missing procedure still returns `SKILL`; the widget's mini-plane now renders effective shared-access-boundary and skill-work needs rather than treating existing access as judgment. Updated the figure, chapter Exercise 03, and artifact README to teach adopt-existing, add-skill-for-procedure, and fresh-data cases. The artifact now regression-tests both `NEITHER` cases.
2. **Round 2, token arithmetic and specification wording.** Corrected `src/chapters/skill-or-server.mdx` and `docs/research/ch11-skill-or-server.md`: 100 Skills at about 100 metadata tokens each cost roughly 10,000 metadata tokens, and a body under 5,000 tokens is a design recommendation rather than a host-enforced maximum.
3. **Round 2, workflow-local script access.** Added the `--script-access` classifier input and mirrored it in `SkillOrServerWidget.tsx`, `SkillOrServerFigure.tsx`, the chapter, artifact README, bundled Skill, formatter documentation, and research record. The model now distinguishes a bare `SKILL.md` from a workflow-local bundled script that uses runtime-provided network access and credentials. It retains MCP for reusable, shared, stateful, or centrally governed access and tests that a local script does not bypass shared governance.
4. **Round 1, live data plus existing access.** Re-verified that an existing CLI or server can fetch fresh data without creating a duplicate server. The updated classifier keeps `--access --live --cli-exists` at `NEITHER` when no procedure is missing and at `SKILL` when judgment is missing.
5. **Round 1, truthful tag behavior.** Re-ran `python3 artifacts/ch11-skill-or-server/hybrid_lab.py --server-only --tag does-not-exist`; it exits nonzero with the visible unsupported-tag error, while the supported fixture range remains `v0.3.0` to `v0.4.0`.
6. **Round 1, valid bundled Skill.** Re-verified that the `release-notes/` directory matches `name: release-notes` and that `artifacts/ch11-skill-or-server/check.sh` runs the portable validator successfully.
7. **Round 1, product claims and sources.** Re-verified the chapter still distinguishes historical Claude Code automatic deferral, current Claude Code configuration, and caller-selected API `defer_loading`; the direct Ronacher and Galarza sources remain listed.
8. **Round 1, current Sentry example.** Re-verified that the chapter and source list use `sentry-get-started`, `sentry-instrument`, and `sentry-debug-issue`.

No advisories were taken. The CONVENTIONS precedence note and the optional direct measurement for the GitHub MCP token estimate remain unchanged.

## Round 3 review (2026-07-16)
Independent re-review: read `prompts/critique-rubric.md`, the complete chapter, figure, widget, runnable artifact, build notes, research backbone, registry/queue state, and the full append-only history with `git log -p -- content/critiques/skill-or-server.md`. Ran `npm run check` successfully, including the artifact's 24 assertions and bundled-skill validation; also exercised the classifier's shared-governance cases. Checked the linked Agent Skills specification and client guide, MCP architecture/transports/authorization specifications, current Claude Code Tool Search documentation and changelog, Anthropic Tool Search material, Ronacher, Galarza, Sentry for AI, JFrog, Willison, Goose, Speakeasy, and Sumo Logic. Re-verified every Round 1 and 2 REQUIRED correction: existing CLI/server access covers live fetches without a duplicate server, unsupported tags fail visibly, the bundled skill validates, Tool Search claims remain scoped and sourced, Sentry uses its current core skills, existing access without procedure routes to neither, metadata arithmetic and the body-size recommendation are accurate, and workflow-local scripts have their own access route.

## Required fixes

1. **`artifacts/ch11-skill-or-server/hybrid_lab.py`, `src/chapters/_widgets/SkillOrServerWidget.tsx`, `src/chapters/_figures/SkillOrServerFigure.tsx`, `src/chapters/skill-or-server.mdx`, and the mirrored artifact documentation --- shared governance is incorrectly treated as a missing server access boundary.** `python3 artifacts/ch11-skill-or-server/hybrid_lab.py --decide --judgment --shared` returns `BOTH`, and `--decide --shared` returns `SERVER`, because both mirrored classifiers unconditionally set the server/access axis from `shared`. Yet the visible question defines `shared` only as serving many agents or clients under central governance, while the figure's horizontal axis is explicitly a **new shared / reusable access** boundary. A centrally provisioned review policy, runbook, or brand-guidelines Skill has judgment work but no access gap and must remain `SKILL`; the official [Agent Skills client guide](https://agentskills.io/client-implementation/adding-skills-support) explicitly supports organization-wide admin deployment and external provisioning of Skills. Narrow this input to an unmet shared/reusable *access* boundary, or split Skill distribution/governance from access governance. Then make the chapter, figure, widget, README, research record, and regression cases teach the corrected distinction, including a passing `judgment + shared Skill distribution -> SKILL` case while retaining the server route for genuinely shared access.

## Advisories

- The financial-services and life-sciences deployment anecdote needs its direct source. The linked Anthropic engineering article supports Skills complementing MCP generally, but not that precise deployment account. This is illustrative rather than load-bearing, so it does not block this round.

## Builder resolution (2026-07-16)
Regression gate: read the complete append-only history with `git log -p -- content/critiques/skill-or-server.md`, then re-verified every REQUIRED finding from Rounds 1, 2, and 3 against the current chapter, figure, widget, runnable artifact, and matching research backbone. `npm run check` passes all seven stages; the Chapter 11 artifact check passes 26 assertions and validates the bundled `release-notes` Skill.

1. **Round 3, Skill distribution is not a server access gap.** Split the classifier and widget input into `shared_access` / `--shared-access`, which means an unmet reusable access adapter, and `skill_distributed` / `--skill-distributed`, which means central provisioning of instruction. Updated `hybrid_lab.py`, `SkillOrServerWidget.tsx`, `SkillOrServerFigure.tsx`, the chapter, artifact README, and research record. The figure now plots a centrally delivered review policy in the Skill region. New artifact regressions prove `--decide --judgment --skill-distributed` returns `SKILL`, an unmet shared access adapter returns `SERVER`, and that adapter plus judgment returns `BOTH`.
2. **Round 1 re-verification.** Existing CLI or server access still covers fresh data without a duplicate server; the unsupported fixture tag still fails visibly; the `release-notes/` directory still matches and validates its Skill name; the chapter still separates Claude Code and API Tool Search behavior and lists Ronacher and Galarza; and the Sentry example still uses `sentry-get-started`, `sentry-instrument`, and `sentry-debug-issue`.
3. **Round 2 re-verification.** Existing non-live CLI access with no procedure still returns `NEITHER`; 100 Skills still cost roughly 10,000 metadata tokens and the 5,000-token body is still described as a recommendation; workflow-local scripts with runtime credentials still route to `SKILL` without bypassing a genuinely unmet shared access adapter.

No advisories were taken. The Round 3 deployment anecdote remains unchanged because it is non-blocking. `npm run check` completed successfully after these changes.
